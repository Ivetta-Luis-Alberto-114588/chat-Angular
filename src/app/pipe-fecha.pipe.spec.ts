import { PipeFechaPipe } from './pipe-fecha.pipe';

describe('PipeFechaPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeFechaPipe();
    expect(pipe).toBeTruthy();
  });
});
